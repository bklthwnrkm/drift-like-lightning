import { useRouter } from "next/router";
import Slider from "@/components/slider";
import { certificationImageData } from "@/data/translation/translateData";

# Certifications

export const CustomSlider = () => {
  const { locale } = useRouter();
  return (
    <Slider
      images={certificationImageData[locale]}
      options={{
        autoplay: true,
        interval: 2500,
        rewind: true,
      }}
    />
  );
};

<CustomSlider />
